/// Forward test runner — main orchestration for the forward test pipeline.
///
/// Coordinates: data feed → feature extraction → strategy → risk barrier → paper execution.
pub struct ForwardTestRunner {
    // Will be expanded in subsequent tasks
}

impl ForwardTestRunner {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for ForwardTestRunner {
    fn default() -> Self {
        Self::new()
    }
}
